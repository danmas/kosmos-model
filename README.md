# Веб-интерфейс для тестирования и взаимодействия с различными API искусственного интеллекта.
AI Analytics Interface

ВАЖНО!!! База Знаний(набор README... файлов) находится в папке ./KB . При формировании ответов в первую очередь обращайся туда.



## 🚀 Последние обновления

### Модели по умолчанию и быстрый выбор (новинка!)

Добавлена поддержка **предустановленных моделей по умолчанию**:

- 💰 **CHEAP** - бесплатная модель для простых запросов
- ⚡  **FAST** - сверхбыстрая модель от GROQ для оперативных ответов
- 💎 **RICH** - мощная модель для сложных задач

**Особенности:**
- 🔄 **Удобный селектор** для быстрого переключения между типами моделей
- 🔗 **Синхронизация** с обычным селектором моделей
- 💾 **Сохранение выбора** между сессиями
- 🧩 **Простой API** - используйте ключевые слова "CHEAP", "FAST", "RICH" вместо полных названий моделей

### Страница доступных моделей

Добавлена **сервисная страничка для просмотра всех доступных AI моделей**:

- 🧠 **Удобный просмотр** всех моделей GROQ и OpenRouter
- 📋 **Быстрое копирование** названий моделей для настроек
- 🔍 **Поиск и фильтрация** по названиям и провайдерам
- 📊 **Статистика** по категориям моделей
- 🎨 **Современный интерфейс** с цветовым разделением провайдеров
- 📱 **Адаптивный дизайн** для всех устройств

**Доступ**: Кнопка "🧠 Доступные модели" в шапке приложения или `/models.html`

### Интеграция с GROQ API

Добавлена поддержка **GROQ API** - сверхбыстрого провайдера для инференса LLM моделей:

- ⚡ **В 5-10 раз быстрее** традиционных провайдеров
- 🆓 **Щедрые бесплатные лимиты** 
- 🤖 **Топовые модели**: Llama 3.3 70B, Mixtral 8x7B, Gemma 2 9B
- 🔄 **Автоматический выбор провайдера** по модели
- 📊 **Единая статистика** для всех провайдеров
- 🧠 **Поддержка RAG** для обоих провайдеров (OpenRoute + GROQ)

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```
4. Отредактируйте файл `.env` и добавьте свои настройки

## Переменные окружения

Проект использует библиотеку `dotenv` для загрузки переменных окружения из файла `.env`. Вот список доступных переменных:

| Переменная | Описание | Пример значения |
|------------|----------|-----------------|
| N8N_WEBHOOK_URL | URL веб-хука N8N | http://localhost:5678/webhook/m1 |
| N8N_WEBHOOK_TEST_URL | URL тестового веб-хука N8N | http://localhost:5678/webhook-test/m1 |
| PORT | Порт, на котором будет запущен сервер | 3002 |
| LOG_LEVEL | Уровень логирования | info |
| IS_TEST_MODE | Режим тестирования | false |
| OPENROUTER_API_KEY | API-ключ для OpenRouter | sk-your-api-key |
| **GROQ_API_KEY** | **API-ключ для GROQ** | **gsk-your-groq-key** |
| OPENAI_API_KEY | API-ключ для OpenAI (опционально) | sk-your-openai-key |
| SUPABASE_URL | URL для Supabase (опционально) | https://your-project.supabase.co |
| SUPABASE_KEY | Ключ для Supabase (опционально) | your-supabase-key |
| LANGCHAIN_PG_URL | URL сервиса Langchain PG | http://localhost:3005 |
| LANGCHAIN_PG_ENABLED | Включить поддержку Langchain PG | true |
| OUTPUT_DOCS_DIR | Директория для сохранения документов | ./output_docs |
| **DEFAULT_MODEL_CHEAP** | **Модель по умолчанию для типа CHEAP (новинка!)** | **google/gemini-2.0-flash-exp:free** |
| **DEFAULT_MODEL_CHEAP_PROVIDER** | **Провайдер для CHEAP модели** | **openroute** |
| **DEFAULT_MODEL_FAST** | **Модель по умолчанию для типа FAST** | **llama3-70b-8192** |
| **DEFAULT_MODEL_FAST_PROVIDER** | **Провайдер для FAST модели** | **groq** |
| **DEFAULT_MODEL_RICH** | **Модель по умолчанию для типа RICH** | **google/gemini-2.5-pro-exp-03-25** |
| **DEFAULT_MODEL_RICH_PROVIDER** | **Провайдер для RICH модели** | **openroute** |

## Управление моделями

С последним обновлением управление списками моделей для **GROQ** и **OpenRouter** было вынесено во внешние конфигурационные файлы для удобства и гибкости.

- **Модели GROQ**: настраиваются в файле `groq-models.json`
- **Модели OpenRouter**: настраиваются в файле `openrouter-models.json`

Для добавления, удаления или изменения модели достаточно отредактировать соответствующий JSON-файл. Сервер автоматически подхватит изменения при следующем запуске.

### Формат данных в `*.json` файлах

Каждая модель описывается объектом со следующими полями:

- `name` (string, обязательное): Уникальное имя модели, используемое в API.
- `visible_name` (string): Отображаемое имя в интерфейсе.
- `provider` (string, обязательное): `"groq"` или `"openroute"`.
- `showInApi` (boolean): Показывать ли модель в списке `/api/available-models`.
- `use_in_ui` (boolean): Доступна ли модель для выбора в UI.
- `context` (number): Максимальный размер контекста в токенах.
- `fast` (boolean): Является ли модель высокопроизводительной (для UI).

**Пример (`groq-models.json`):**
```json
{
  "models": [
    { 
      "name": "llama-3.3-70b-versatile", 
      "visible_name": "Llama 3.3 70B (Versatile)", 
      "provider": "groq",
      "context": 8192,
      "fast": true
    }
  ]
}
```

## Запуск

```bash
node server.js
```

После запуска приложение будет доступно по адресу http://localhost:3002 (или порту, указанному в переменных окружения).

## Функции

### 🧠 Управление моделями
- 📋 **Сервисная страница моделей** - просмотр и копирование названий всех доступных моделей
- 🔍 **Поиск и фильтрация** моделей по провайдерам и названиям
- 📊 **Статистика моделей** по категориям (GROQ/OpenRouter, быстрые/обычные)
- 🎨 **Визуальное разделение** провайдеров цветовыми схемами

### 🚀 Основной функционал
- 🔄 **Мультипровайдерная поддержка**: OpenRoute + GROQ API
- 🤖 Отправка запросов к различным моделям AI
- 📝 Сохранение и управление промптами
- 📚 История запросов и ответов (с указанием провайдера)
- 🧠 Поддержка Retrieval Augmented Generation (RAG)
- 💾 Возможность сохранения ответов в формате Markdown 
- ⚡ **Автоматический выбор провайдера** по модели
- 🛡️ Расширенная обработка ошибок с передачей детальной информации клиенту
- 📊 Статистика использования токенов для всех провайдеров

## API

### POST /api/send-request

Отправляет запрос к модели AI и возвращает ответ.

**Параметры запроса:**
- `model`: Название модели для использования. Может быть:
  - Полное название модели (например, `"llama3-70b-8192"`)
  - Ключевое слово типа модели (`"CHEAP"`, `"FAST"` или `"RICH"`)
  - Пустая строка или отсутствует (будет использована модель типа `CHEAP` по умолчанию)
- `prompt`: Промпт для модели.
- `inputText`: Входной текст для модели.
- `provider` (необязательно): Принудительный выбор провайдера (`"groq"` или `"openroute"`). Если не указан, определяется автоматически по модели.
- `useRag` (необязательно): Использовать RAG для обогащения контекста.
- `contextCode` (необязательно): Код контекста для RAG.
- `saveResponse` (необязательно): Логическое значение, указывающее, следует ли сохранять ответ в историю. По умолчанию `false`.

**Примеры использования:**

**Использование ключевого слова FAST (быстрая модель):**
```bash
curl -X POST http://localhost:3002/api/send-request \
-H "Content-Type: application/json" \
-d '{"model": "FAST", "prompt": "Ты полезный помощник", "inputText": "Привет!", "saveResponse": true}'
```

**Использование модели по умолчанию (CHEAP):**
```bash
curl -X POST http://localhost:3002/api/send-request \
-H "Content-Type: application/json" \
-d '{"prompt": "Ты полезный помощник", "inputText": "Привет!", "saveResponse": true}'
```

**Принудительный выбор GROQ:**
```bash
curl -X POST http://localhost:3002/api/send-request \
-H "Content-Type: application/json" \
-d '{"model": "llama-3.3-70b-versatile", "prompt": "Ты полезный помощник", "inputText": "Привет!", "provider": "groq", "saveResponse": true}'
```

**С использованием RAG:**
```bash
curl -X POST http://localhost:3002/api/send-request \
-H "Content-Type: application/json" \
-d '{"model": "RICH", "prompt": "Ответь на основе контекста", "inputText": "Вопрос", "useRag": true, "contextCode": "docs"}'
```

### POST /api/send-request-sys

Отправляет запрос к модели AI, используя промпт, выбранный по имени из хранилища промптов.

**Параметры запроса:**
- `model`: Название модели для использования. Может быть:
  - Полное название модели (например, `"llama3-70b-8192"`)
  - Ключевое слово типа модели (`"CHEAP"`, `"FAST"` или `"RICH"`)
  - Пустая строка или отсутствует (будет использована модель типа `CHEAP` по умолчанию)
- `prompt_name`: Имя системного промпта из хранилища.
- `inputText`: Входной текст для модели.
- `provider` (необязательно): Принудительный выбор провайдера (`"groq"` или `"openroute"`). Если не указан, определяется автоматически по модели.
- `saveResponse` (необязательно): Логическое значение, указывающее, следует ли сохранять ответ в историю. По умолчанию `true`.

### POST /analyze

Альтернативный маршрут для отправки запросов к AI моделям с поддержкой RAG.

**Параметры запроса:**
- `model`: Название модели для использования. Может быть:
  - Полное название модели (например, `"llama3-70b-8192"`)
  - Ключевое слово типа модели (`"CHEAP"`, `"FAST"` или `"RICH"`)
  - Пустая строка или отсутствует (будет использована модель типа `CHEAP` по умолчанию)
- `prompt`: Промпт для модели.
- `inputText`: Входной текст для модели.
- `provider` (необязательно): Принудительный выбор провайдера.
- `useRag` (необязательно): Использовать RAG для обогащения контекста.
- `contextCode` (необязательно): Код контекста для RAG.

### Другие доступные маршруты

Приложение также предоставляет API для:
- Управления промптами (`/api/prompts`)
- Работы с историей ответов (`/api/responses`)
- Взаимодействия с RAG (`/api/rag/*`)
- Работы с файлами (`/api/save-markdown`, `/api/output-dir-info`)
- **Просмотра моделей** (`/api/all-models`, `/models.html`)

Полная документация API доступна в файле [README_AIAN_MODEL_REST.md](README_AIAN_MODEL_REST.md).

### GET /api/available-models

Возвращает список доступных моделей (только названия).

**Пример использования:**

```bash
curl -X GET http://localhost:3002/api/available-models
```

### GET /api/all-models

Возвращает полную информацию о всех доступных моделях с метаданными.

**Пример использования:**

```bash
curl -X GET http://localhost:3002/api/all-models
```

### GET /api/default-models

Возвращает информацию о моделях по умолчанию (CHEAP, FAST, RICH).

**Пример использования:**

```bash
curl -X GET http://localhost:3002/api/default-models
```

**Пример ответа:**
```json
{
  "success": true,
  "defaultModels": {
    "cheap": {
      "model": "google/gemini-2.0-flash-exp:free",
      "provider": "openroute",
      "description": "Бесплатная модель для простых запросов"
    },
    "fast": {
      "model": "llama3-70b-8192",
      "provider": "groq",
      "description": "Быстрая модель для оперативных ответов"
    },
    "rich": {
      "model": "google/gemini-2.5-pro-exp-03-25",
      "provider": "openroute",
      "description": "Мощная модель для сложных задач"
    }
  }
}
```

### GET /api/default-models/:type

Возвращает информацию о конкретной модели по умолчанию (CHEAP, FAST или RICH).

**Пример использования:**

```bash
curl -X GET http://localhost:3002/api/default-models/fast
```

### POST /api/default-models

Устанавливает новую модель по умолчанию для указанного типа.

**Параметры запроса:**
- `type`: Тип модели (`"cheap"`, `"fast"` или `"rich"`).
- `model`: Название модели.
- `provider`: Провайдер модели (`"groq"` или `"openroute"`).

**Пример использования:**

```bash
curl -X POST http://localhost:3002/api/default-models \
-H "Content-Type: application/json" \
-d '{"type": "fast", "model": "mixtral-8x7b-32768", "provider": "groq"}'
```

### GET /models.html

**Веб-страница для просмотра и копирования моделей** - удобный интерфейс для пользователей сервиса.

**Особенности:**
- 🧠 Группировка по провайдерам (GROQ/OpenRouter)
- 📋 Однокликовое копирование названий моделей
- 🔍 Поиск и фильтрация в реальном времени
- 📊 Статистика по категориям моделей
- 📱 Адаптивный дизайн

**Доступ:** `http://localhost:3002/models.html` или через кнопку в главном приложении

### Пример ответа /api/available-models

```json
[
  "llama-3.3-70b-versatile",
  "llama3-8b-8192", 
  "llama3-70b-8192",
  "mixtral-8x7b-32768",
  "gemma2-9b-it",
  "google/gemma-3-27b-it:free",
  "google/gemini-2.0-flash-exp:free",
  "google/gemini-2.0-flash-lite-preview-02-05:free",
  "google/gemini-2.0-flash-thinking-exp-1219:free",
  "google/gemini-2.0-pro-exp-02-05:free",
  "google/gemini-2.0-flash-thinking-exp:free",
  "deepseek/deepseek-chat:free",
  "deepseek/deepseek-r1-zero:free",
  "deepseek/deepseek-r1-distill-llama-70b:free",
  "deepseek/deepseek-r1:free",
  "qwen/qwq-32b:free",
  "qwen/qwen2.5-vl-72b-instruct:free",
  "moonshotai/moonlight-16b-a3b-instruct:free",
  "nousresearch/deephermes-3-llama-3-8b-preview:free",
  "cognitivecomputations/dolphin3.0-r1-mistral-24b:free",
  "cognitivecomputations/dolphin3.0-mistral-24b:free",
  "sophosympatheia/rogue-rose-103b-v0.2:free",
  "qwen/qwen-2.5-coder-32b-instruct:free",
  "nvidia/llama-3.1-nemotron-70b-instruct:free",
  "gryphe/mythomax-l2-13b:free"
]
```

*Первые 5 моделей - это новые GROQ модели, отличающиеся высокой скоростью работы*

### Расширенная обработка ошибок

Все API-эндпоинты, связанные с вызовом моделей AI (`/api/send-request`, `/api/send-request-sys`, `/analyze`), возвращают расширенную информацию об ошибках:

**Пример ответа при некорректной структуре ответа от AI модели:**
```json
{
  "error": "Invalid response from AI model",
  "data": {
    // Содержит полные данные ответа от API
  }
}
```

**Пример ответа при других ошибках:**
```json
{
  "error": "API Error: 429 - Rate limit exceeded",
  "details": {
    // Подробная информация об ошибке
    "error": "rate_limit_exceeded",
    "message": "Your API requests are being rate-limited"
  }
}
```

Это позволяет клиентским приложениям получать больше информации о возникших ошибках и лучше их обрабатывать.

## 🚀 Поддерживаемые провайдеры

### GROQ API (рекомендуется для скорости)
- **Скорость**: В 5-10 раз быстрее традиционных API
- **Модели**: Llama 3.3 70B, Llama 3 8B/70B, Mixtral 8x7B, Gemma 2 9B
- **Бесплатные лимиты**: Щедрые квоты для разработки
- **Получить ключ**: https://console.groq.com/

### OpenRouter API (разнообразие моделей)
- **Модели**: 20+ различных моделей от Google, DeepSeek, Qwen и др.
- **Бесплатные модели**: Много free-tier моделей
- **Получить ключ**: https://openrouter.ai/

## 📊 Пример расширенного ответа

При использовании любого провайдера вы получите подробную информацию:

```json
{
  "success": true,
  "content": "Привет! Как дела?",
  "model": "llama-3.3-70b-versatile",
  "provider": "groq",
  "usage": {
    "prompt_tokens": 15,
    "completion_tokens": 8,
    "total_tokens": 23
  },
  "rag": null
}
```

## 🔧 Быстрый старт с GROQ

1. Получите GROQ API ключ на https://console.groq.com/
2. Добавьте в `.env` файл:
   ```
   GROQ_API_KEY=gsk_ваш_ключ_здесь
   ```
3. Запустите сервер: `npm start`
4. Выберите любую модель с префиксом "🚀 GROQ:" в веб-интерфейсе
5. Наслаждайтесь молниеносными ответами! ⚡ 